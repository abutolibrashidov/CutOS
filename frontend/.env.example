# URL of the backend API (used in production builds)
# In development, Vite proxies /api requests to localhost:8000 automatically.
VITE_API_URL=http://localhost:8000
# Only for local backend development. Never use in production.
# VITE_DEV_TELEGRAM_ID=123456
